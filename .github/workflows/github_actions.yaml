name: push-workflow

on: [pull_request]

jobs:
  tests:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v3
#      - name: Pytest installation
#        run: pip install pytest
#      - name: Run tests
#        run: |
#          set ENV=TEST
#          python pytest .
      - run: set "PYTHONPATH=%cd%\src"
      - run: echo %PYTHONPATH%
#      - run: pip install -r requirements.txt
#      - run: set ENV=TEST
#      - run: pytest